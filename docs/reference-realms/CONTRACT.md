# Reference realm implementation contract

Supersedes humanoid geometry. Worktree: /home/rayanfahil2/asgard-bifrost, current main b6db89a. Root/public index mirrored; backend src/ and wrangler.toml preserved. Development reference pack remains outside public/.

Coordinates: Y up, X screen horizontal, +Z toward camera. Floor Y=0. Default perspective camera roughly (0,10,15), target (0,1,0); integration tunes framing by viewport. Council anchors in CAST ordering top/right/lower-right/lower-left/left: (0,0,-4.35), (4,0,-.4), (3.4,0,3.3), (-3.4,0,3.3), (-4,0,-.4). Shared advisor gemstones height1.55, width.72, platform top .65; gem bottom .82. Rear gem can scale 1.10 to compensate perspective. Main body's projected height target1.4–1.8x gem; main body physical height~2.4, foundation excluded. All five advisors supplied by integration, not model factories.

Factory files: public/ui/realm-thor.js export createThor; realm-loki.js export createLoki; realm-odin.js export createOdin. API factory(THREE,{quality='balanced',preview=false}={}) returns {root,hero,update(time,animated,state),dispose(),stats?}. Factory adds no global scene/listeners/RAF. root owns everything; hero is a Group holding only movable centerpiece (not full foundation). Integration can move hero.position within modest bounds; updates animate children, never overwrite hero.position. Dispose unique owned geometries/materials/textures and remove root; no cross-factory shared resources. All geometry local JS; no reference JPG imports. Main footprint radius<=2.5, total main height<=4.3. Odin may own larger ground plaza radius5.5 with advisor bridges/stairs following anchors, but no advisor gemstones/platforms (integration supplies). No per-frame allocations/resources/network. Named component groups for measurable independent motion. Material meshes use lit Standard/Physical restrained metal/glow. Heavy details only where visible.

Lead owns scene.js, realm-architecture.js/shared council, index, app integration, CSS, tests, camera/input, deployment. Specialists own only their named model module and brief model notes. No browser/deployment contention; lead captures baseline and integrated output. Factory modules import no shared helper unless included in own file. Build silhouette, construction, bevels and visible material depth; do not stop at generic box/cone primitives.

Integrated visual refinement: rear advisor moved back .35 units to clear the main silhouette; Loki crystal scales 1.18 with its base height retained to reach the projected moderate-dominance target. Camera default elevation .68, distance max(15.5,17/aspect), target Y=.4.
