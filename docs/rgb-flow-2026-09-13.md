# Immediate RGB dispatch and eased color changes

Worker release: `5a992f98-a4ec-42e5-b4fd-a59fa2a62a87`.

The browser now dispatches on the identity/lock mutation without the former 180 ms debounce. Lighting requests no longer wait for wallpaper changes. One continuously running ASGARD Flow effect receives persona events and blends the current RGB values and accent width to the requested palette over 1,200 ms with cubic ease-out. Repeated heartbeats do not restart the blend; a new agent mid-blend starts from the current interpolated colors.

The existing installed palettes were preserved: Thor blue with gold bands; Loki forest green with white bands; Odin orange-gold with white bands; lock-in deep red. Device/channel mapping was not changed. This does not implement exact fan-ring addressing.

The old effect files and helper were backed up under `%LOCALAPPDATA%/ASGARD-RGB/backups/flow-*`. Flow is installed in Documents/WhirlwindFX/Effects; the running helper is updated and retains existing sign-in startup. SignalRGB was restarted once to discover the new effect. Its API confirmed ASGARD Flow active and brightness 100 after verification.

Measured local helper request completion: initial activation 242 ms; subsequent Thor/Loki/Odin requests 33/24/21 ms. These are local software request times, not browser-to-LED measurements. Physical visual smoothness still requires user observation. A sleep-mode switch test confirmed global brightness remained zero; the prior awake state and Odin selection were restored afterward.

Verification: 175 tests passed. Added tests exercise blend start/end, interruption continuity, repeated/foreign events, nonblocking desktop changes and no brightness mutation on the active-effect path. Live website immediate-dispatch code and installed helper/effect hashes verified.

Existing browser tabs need one refresh to load the new dispatch code. Existing pairing remains valid. No additional subscription or hardware purchase is needed.

Uses the official [SignalRGB canvas event API](https://docs.signalrgb.com/developer/lightscripts/creating-dev-integrations/) and [lighting API](https://docs.signalrgb.com/developer/signalrgb-api/lighting/).
