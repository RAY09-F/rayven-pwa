# Bifrost improvements

| Change | User benefit | Evidence |
| --- | --- | --- |
| Broken asymmetric aperture, restrained seam, clean layout | Dominant portrait with readable conversation and fewer default actions | Desktop and phone browser captures |
| Shaded original persona meshes with surface bands | Recognizable volume and silhouette instead of a picture or particle tangle | WebGL compile/paint, still/orbit/grayscale, geometry tests |
| Mesh-shaded software projection | Deliberate sculptural fallback with explicit renderer label | Separate software capture and occlusion tests |
| Real request ownership, stop-waiting, draft recovery | Late replies cannot hijack another persona; errors are recoverable | State/ownership tests and fixture browser interactions |
| Copy, latest-message and focus handling | Better long conversations and keyboard continuity | Browser transcript and dialog tests |
| Mobile viewport-aware composer | Send remains reachable when the keyboard reduces visible height | 390×460 viewport check; physical keyboard remains separate evidence |
| Local action receipts and JSON export | Actual calculation result, timestamp, inputs and next step | Browser calculation/export check |
| Actionable provider failures | The live credit blocker is explained without exposing raw provider details; draft recovery remains available | Safe error classification tests and live HTTP evidence |
| Content-derived release manifest | Live asset identity can be verified independently of deployment claims | SHA-256/MIME verification script |
| Complete searchable backlog index | All 445 capability entries and 40 workflow/request briefs retained without readiness inflation | Deterministic inventory check |

The retained backend is unchanged. No new provider credentials, live trading, billing, messages, or unattended workflows are activated by this release. Deployment evidence belongs in DEPLOYMENT.md; local checks alone are not deployment proof.
