import {createRequire} from 'node:module';import {mkdirSync,writeFileSync} from 'node:fs';
const require=createRequire(process.env.USERPROFILE+'/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/package.json');const {chromium}=require('playwright');const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage({viewport:{width:1440,height:1000}});await p.goto('http://127.0.0.1:4194/');await p.waitForTimeout(1200);mkdirSync('output/mobile',{recursive:true});const rects=await p.locator('.topbar,.shell,.stage,.intro,.collections,.conversation').evaluateAll(es=>es.map(e=>({c:e.className,r:e.getBoundingClientRect().toJSON()})));writeFileSync('output/mobile/desktop-before.json',JSON.stringify(rects));await p.screenshot({path:'output/mobile/desktop-before.png'});await b.close();
