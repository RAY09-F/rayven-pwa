diff --git a/index.html b/index.html
index 71ba8b1..33b1c5c 100644
--- a/index.html
+++ b/index.html
@@ -5581,15 +5581,10 @@ function _h9Loop() {
     blip(2400, 140, 0.05, 'square', 0.03);
     setTimeout(() => tear.classList.remove('rip'), 70 + Math.random() * 110);
   }
-  // A BREACH that isn't one. Everything goes red, the alarm reads, and then it
-  // settles — and you are never quite sure it settled on its own.
-  if (now > (_h9.nextAlarm || (_h9.enteredAt + 40000))) {
-    _h9.nextAlarm = now + 55000 + Math.random() * 70000;
-    const cell = _h9.dom, warn = cell.querySelector('#h9Warn');
-    cell.classList.add('alarm'); warn.classList.remove('on'); void warn.offsetWidth; warn.classList.add('on');
-    blip(300, 240, 0.5, 'square', 0.06); blip(240, 300, 0.5, 'square', 0.06, 0.5); blip(300, 240, 0.5, 'square', 0.06, 1.0);
-    setTimeout(() => { cell.classList.remove('alarm'); warn.classList.remove('on'); }, 2600);
-  }
+  // The fake breach alarm lived here — "CONTAINMENT INTEGRITY FALLING", the
+  // whole cell going red every minute or two. It was atmosphere and it read as
+  // an error. Removed on request. The heartbeat and the murmurs stay; those are
+  // quiet and they are what make the room feel occupied rather than alarmed.
   // a heartbeat under the room, slow, and faster the longer you watch her
   if (now > (_h9.nextBeat || 0)) {
     _h9.nextBeat = now + 2600 - _h9.attention * 1100;
diff --git a/public/index.html b/public/index.html
index 71ba8b1..33b1c5c 100644
--- a/public/index.html
+++ b/public/index.html
@@ -5581,15 +5581,10 @@ function _h9Loop() {
     blip(2400, 140, 0.05, 'square', 0.03);
     setTimeout(() => tear.classList.remove('rip'), 70 + Math.random() * 110);
   }
-  // A BREACH that isn't one. Everything goes red, the alarm reads, and then it
-  // settles — and you are never quite sure it settled on its own.
-  if (now > (_h9.nextAlarm || (_h9.enteredAt + 40000))) {
-    _h9.nextAlarm = now + 55000 + Math.random() * 70000;
-    const cell = _h9.dom, warn = cell.querySelector('#h9Warn');
-    cell.classList.add('alarm'); warn.classList.remove('on'); void warn.offsetWidth; warn.classList.add('on');
-    blip(300, 240, 0.5, 'square', 0.06); blip(240, 300, 0.5, 'square', 0.06, 0.5); blip(300, 240, 0.5, 'square', 0.06, 1.0);
-    setTimeout(() => { cell.classList.remove('alarm'); warn.classList.remove('on'); }, 2600);
-  }
+  // The fake breach alarm lived here — "CONTAINMENT INTEGRITY FALLING", the
+  // whole cell going red every minute or two. It was atmosphere and it read as
+  // an error. Removed on request. The heartbeat and the murmurs stay; those are
+  // quiet and they are what make the room feel occupied rather than alarmed.
   // a heartbeat under the room, slow, and faster the longer you watch her
   if (now > (_h9.nextBeat || 0)) {
     _h9.nextBeat = now + 2600 - _h9.attention * 1100;
